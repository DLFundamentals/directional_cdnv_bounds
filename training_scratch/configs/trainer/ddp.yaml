# ddp.yaml
devices: auto                # set to number of GPUs on the node
accelerator: gpu
strategy: ddp_find_unused_parameters_true
max_epochs: 2000
accumulate_grad_batches: 1
use_distributed_sampler: true

# quality-of-life
num_sanity_val_steps: 0
log_every_n_steps: 50
